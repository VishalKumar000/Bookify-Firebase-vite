/*
    TODO: Query data from Firebase Firestore and Reactjs - Part 2 
    ! 18:00 min
    ? error in order page , fix by useEffect((), [!firebase])
    ? ?.then().catch
    npm run build
    netlify
    ? src={bookData.photoURL === null ? "/DefaultUser.png" : bookData.photoURL}
*/